apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - http-listener.yaml
  - grafana-httproute.yaml
  # kagent-httproute.yaml is applied conditionally by the setup script
  # when OPENAI_API_KEY is set in vars.sh
#  - loki-httproute.yaml
#  - prometheus-httproute.yaml
#  - alertmanager-httproute.yaml
#  - unpoller-httproute.yaml
