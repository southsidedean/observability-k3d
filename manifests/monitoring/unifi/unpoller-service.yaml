apiVersion: v1
kind: Service
metadata:
  name: unpoller
  namespace: monitoring
  labels:
    app: unpoller
spec:
  type: ClusterIP
  ports:
    - name: http-metrics
      port: 9130
      targetPort: metrics
  selector:
    app: unpoller
