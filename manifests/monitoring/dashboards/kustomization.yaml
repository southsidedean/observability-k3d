# manifests/monitoring/dashboards/kustomization.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring # Install dashboards into the monitoring namespace
resources:
  - blackbox-exporter-dashboard.yaml
  - node-exporter-dashboard.yaml